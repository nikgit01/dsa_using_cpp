#include "A1st.cpp"
int greatest(Array obj){
    int max;
    if(obj.isEmpty()){
        cout<<"The array was empty";
        return -1;
    }
    max=obj.get(0);
    for(int i=1; i<obj.count(); i++){

    }
}